import { DetectChangeDirective } from './detect-change.directive';

describe('DetectChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectChangeDirective();
    expect(directive).toBeTruthy();
  });
});
